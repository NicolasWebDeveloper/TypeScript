let movieTitle: string = 'James Bond';
movieTitle.toUpperCase();

let age: number = 1;
age = 10;

const test: boolean = true;

let show = true;

const movies: string[] = ['a', 'b'];
for (const movie of movies) {
  console.log(movie);
}
